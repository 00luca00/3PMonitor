# 3PMonitor
Simple terminal CPU/RAM monitor for Linux (Parrot OS)

Requirements:
- Python 3.8+
- psutil

Install (system-wide):
sudo mv 3pmonitor /usr/local/bin/3pmonitor
sudo chown root:root /usr/local/bin/3pmonitor
sudo chmod 755 /usr/local/bin/3pmonitor
sudo pip3 install psutil
